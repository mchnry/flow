using System;
using System.Collections.Generic;
using System.Text;
using Moq;
using Xunit;

namespace Test.Engine
{
    public class ImplementationManagerTests
    {

        //throws if can't resolve evaluator
        //throws if can't resolve action
        //does not call factory if action exists
        //does not call factory if evaluator exists
        //calls factory if action not found
        //calls factory if evaluator not found

    }
}
